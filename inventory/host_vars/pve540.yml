---
# Migration window (tofu-proxmox#816): keep the roles active solely to
# reconcile any scheduler drift to absent.  The timer is stopped and disabled;
# the empty job list removes both Syncoid scheduling paths.
sanoid_enabled: true
sanoid_timer_enabled: false
syncoid_enabled: true
syncoid_jobs: []
