---
# Host network contract role metadata

galaxy_info:
  role_name: pve_host_network
  author: ansible-proxmox
  description: Detect (and optionally enforce) the host network contract
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
dependencies: []
