---
# wol_enable role metadata

galaxy_info:
  role_name: wol_enable
  author: ansible-proxmox
  description: Persist Wake-on-LAN (magic packet) on a host's physical NIC across reboots
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie

  galaxy_tags:
    - proxmox
    - networking
    - power

dependencies: []
