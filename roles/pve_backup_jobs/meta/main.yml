---
# PVE backup jobs role metadata

galaxy_info:
  role_name: pve_backup_jobs
  author: ansible-proxmox
  description: Reconcile cluster vzdump backup jobs from a declared pool-scoped desired state
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie

  galaxy_tags:
    - proxmox
    - backup

dependencies: []
