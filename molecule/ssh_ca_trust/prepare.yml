---
- name: Prepare
  hosts: all
  gather_facts: false

  tasks:
    - name: Install Python for Ansible modules
      ansible.builtin.raw: |
        apt-get update && apt-get install -y python3
      changed_when: true
