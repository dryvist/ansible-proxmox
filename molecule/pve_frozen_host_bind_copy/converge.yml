---
- name: Converge
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Include the inert frozen host-bind copy role
      ansible.builtin.include_role:
        name: pve_frozen_host_bind_copy
